[user]
  name = Jonathan Gin
  email = jonathangin52@gmail.com
[core]
  editor = vim
  excludesfile = /Users/jonathan/.gitignore_global
	commitGraph = true
[alias]
  co = checkout
  hist = log --graph --pretty='''%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset''' --all
  cleanup = "!git branch --merged | rg  -v '\\*\\|master\\|develop' | xargs -n 1 git branch -d"
  ctags = !.git/hooks/ctags
  pushf = push --force-with-lease
[diff]
  algorithm = patience
[protocol]
  version = 2
[credential]
  helper = osxkeychain
[url "https://github.com/Shopify/"]
	insteadOf = git@github.com:Shopify/
	insteadOf = git@github.com:shopify/
	insteadOf = ssh://git@github.com/Shopify/
	insteadOf = ssh://git@github.com/shopify/
[gc]
	writeCommitGraph = true
